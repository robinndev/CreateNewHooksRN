import productorsMock from "../mocks/productors";
import { topMock } from "../mocks/top";

export const loadProductors = () => {
    return productorsMock
}

export const loadTop = () => {
    return topMock
}